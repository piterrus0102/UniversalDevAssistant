# 🚀 Quick Start Guide

## За 5 минут запусти AI-ассистента для своего проекта!

### Шаг 1: Получи Claude API ключ

1. Иди на https://console.anthropic.com/
2. Зарегистрируйся (бесплатно)
3. Создай API ключ в разделе "API Keys"
4. Скопируй его

### Шаг 2: Настрой переменную окружения

**macOS / Linux:**
```bash
export ANTHROPIC_API_KEY="sk-ant-твой-ключ-здесь"
```

**Или добавь в `~/.zshrc` или `~/.bashrc`:**
```bash
echo 'export ANTHROPIC_API_KEY="sk-ant-твой-ключ"' >> ~/.zshrc
source ~/.zshrc
```

### Шаг 3: Создай config.yaml

```bash
cd /Users/ruslanhafizov/Desktop/UniversalDevAssistant
cp config.yaml.example config.yaml
nano config.yaml  # или открой в любом редакторе
```

**Измени путь к твоему проекту:**

```yaml
project:
  name: "zayobushek"
  path: "/Users/ruslanhafizov/Desktop/zayobushek"  # ← ЗДЕСЬ ТВОЙ ПУТЬ
  docs:
    - "README.md"
    - "docs/*.md"
```

### Шаг 4: Запусти!

```bash
./gradlew run
```

Если увидишь ошибку про Java, установи JDK 17+:
```bash
brew install openjdk@17
```

### Шаг 5: Проверь работу

**Терминал 2:**
```bash
# Простой тест
curl http://localhost:3002/health

# Проверка git
curl http://localhost:3002/git/branch

# Задай вопрос!
curl 'http://localhost:3002/help?q=структура проекта'
```

---

## 🎯 Примеры вопросов для `/help`

```bash
# О структуре
curl 'http://localhost:3002/help?q=как устроен проект'

# О конкретных файлах
curl 'http://localhost:3002/help?q=что делает AuthController'

# О API
curl 'http://localhost:3002/help?q=где документация API'

# Как что-то сделать
curl 'http://localhost:3002/help?q=как добавить новый урок'
```

---

## 🔧 Если что-то не работает

### Ошибка: "ANTHROPIC_API_KEY is not set"
```bash
# Проверь, что переменная установлена
echo $ANTHROPIC_API_KEY

# Если пусто - установи заново
export ANTHROPIC_API_KEY="sk-ant-твой-ключ"
```

### Ошибка: "Config file not found"
```bash
# Убедись, что config.yaml существует
ls -la config.yaml

# Если нет - создай из примера
cp config.yaml.example config.yaml
```

### Ошибка: "Git command failed"
```bash
# Проверь, что путь к проекту правильный
ls /Users/ruslanhafizov/Desktop/zayobushek

# Проверь, что это git репозиторий
cd /Users/ruslanhafizov/Desktop/zayobushek
git status
```

### Ошибка: "Java not found"
```bash
# Установи JDK 17+
brew install openjdk@17

# Или скачай с https://adoptium.net/
```

---

## 📱 Использование из Android/мобильного приложения

Сервер доступен по `http://localhost:3002` с компьютера.

Для Android эмулятора используй: `http://10.0.2.2:3002`

Для реального устройства в одной сети: `http://<IP-твоего-компа>:3002`

---

## 🎓 Для AIAdvent - День 20

Этот проект **полностью соответствует требованиям**:

✅ **RAG** - индексирует README.md + docs/  
✅ **MCP** - выполняет `git branch` и другие git команды  
✅ **Команда /help** - отвечает на вопросы о проекте  

**Минимальные требования для зачета выполнены:**
- ✅ README + 1 папка docs в RAG
- ✅ MCP только для получения текущей ветки (git branch)
- ✅ Команда /help отвечает на вопросы о структуре проекта

**Бонусные фичи:**
- ✅ Полная Git интеграция (status, commits, diff)
- ✅ REST API со множеством endpoint'ов
- ✅ Красивое логирование
- ✅ Универсальность - работает с любым проектом
- ✅ Написано на Kotlin (не JS!)

---

Готово! Теперь у тебя есть персональный AI-ассистент для проекта! 🎉

