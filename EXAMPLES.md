# 🎯 Примеры использования Universal Dev Assistant

## 🚀 Быстрый старт

### 1. Запуск сервера (в одном терминале)
```bash
./gradlew run
```

### 2. Использование (в другом терминале)

## 📝 Два способа работы:

### Способ 1: Прямые команды
```bash
# Задать вопрос
./scripts/assistant.sh ask "что это за проект"

# Git информация
./scripts/assistant.sh git

# Список документов
./scripts/assistant.sh docs
```

### Способ 2: Интерактивный режим (ЛУЧШИЙ! 🌟)
```bash
./scripts/assistant.sh
```

---

## 💬 Интерактивный режим

После запуска `./scripts/assistant.sh` вы попадете в интерактивную консоль:

```
╔════════════════════════════════════════════════════════╗
║  🤖 Universal Dev Assistant                         ║
║  Интерактивный режим                                 ║
╚════════════════════════════════════════════════════════╝

💡 Команды начинаются с /
   /help - справка, /git - статус, /docs - документы, /exit - выход

💬 Просто напишите вопрос без / чтобы спросить AI

> _
```

### ✅ Правильное использование:

#### Вопросы к AI (без `/`):
```
> где API документация
🤖 Ответ: API документация находится в файле 03_API_ДОКУМЕНТАЦИЯ.md...

> как добавить новый урок
🤖 Ответ: Для добавления нового урока...

> что делает AuthController
🤖 Ответ: AuthController отвечает за аутентификацию...
```

#### Команды (с `/`):
```
> /git
📂 Git информация
Текущая ветка: main
✅ Нет изменений

> /docs
📚 Проиндексированные документы
Всего документов: 8
  1. README.md
  2. docs/04_ДОБАВЛЕНИЕ_НОВЫХ_УРОКОВ.md
  ...

> /branch
Текущая ветка: main

> /health
🏥 Состояние сервера
Статус: ok
Проект: zayobushek
Документов: 8

> /exit
Пока! 👋
```

---

## 🎯 Примеры вопросов к AI

### О структуре проекта:
```
> что это за проект
> как устроена структура проекта
> где frontend код
> где backend API
```

### О документации:
```
> где API документация
> где найти примеры использования API
> какие есть endpoints
```

### Как что-то сделать:
```
> как добавить новый урок
> как создать новую секцию
> как запустить проект локально
> как сделать миграцию базы данных
```

### О конкретных файлах:
```
> что делает AuthController
> что в файле SectionController
> зачем нужен migrate_android_studio_course.sql
```

### Технические вопросы:
```
> как работает аутентификация
> какие таблицы есть в базе данных
> какие связи между сущностями
> как организован роутинг
```

---

## 🔧 Все команды в интерактивном режиме

| Команда | Описание | Пример |
|---------|----------|--------|
| `/help` | Показать справку | `> /help` |
| `/git` | Git статус (ветка, изменения, коммиты) | `> /git` |
| `/branch` | Только текущая ветка | `> /branch` |
| `/docs` | Список проиндексированных документов | `> /docs` |
| `/health` | Проверка работы сервера | `> /health` |
| `/exit` | Выход (или `/quit`, `/q`) | `> /exit` |
| *(без `/`)* | Вопрос к AI | `> где API` |

---

## 📋 Шпаргалка

```bash
# 1. Запуск сервера (терминал 1)
./gradlew run

# 2. Интерактивный режим (терминал 2)
./scripts/assistant.sh

# В консоли:
# - Пишите вопросы БЕЗ /
# - Команды С /
# - /exit для выхода
```

---

## 🎨 Что делает каждая команда:

### `/git` - Полная Git информация
- Текущая ветка
- Последний коммит
- Изменения в файлах

### `/branch` - Только ветка
Быстрый способ узнать текущую ветку

### `/docs` - Список документов
Показывает все проиндексированные файлы:
- README.md
- Все файлы из docs/
- Другие markdown файлы

### `/health` - Статус сервера
- Работает ли сервер
- Какой проект подключен
- Сколько документов проиндексировано
- Включен ли Git

---

## 💡 Советы

1. **Используйте интерактивный режим** - намного удобнее чем curl
2. **Задавайте конкретные вопросы** - AI лучше отвечает на детальные вопросы
3. **Используйте `/git` перед коммитами** - быстро проверить изменения
4. **`/docs` покажет что проиндексировано** - AI знает только об этих файлах

---

## 🚀 Готово!

Теперь просто запустите:
```bash
./scripts/assistant.sh
```

И начинайте задавать вопросы! 🎉



