# 🔒 Безопасность и секреты

## ⚠️ Важно: Секреты не должны попадать в Git!

### Файлы с секретами (уже в .gitignore):

- ✅ `config.yaml` - содержит ваш HuggingFace API ключ
- ✅ `local.properties` - локальные настройки Gradle
- ✅ `.env` - переменные окружения

### Перед коммитом проверьте:

```bash
# Убедитесь, что config.yaml НЕ будет закоммичен
git status

# Проверьте, что нет секретов в файлах
grep -r "hf_" --exclude-dir=.git --exclude=config.yaml .
```

## 🔑 Где получить API ключи:

### HuggingFace API Key

1. Зайдите на https://huggingface.co/settings/tokens
2. Создайте новый токен (Read access достаточно)
3. Скопируйте токен
4. Добавьте в `config.yaml`:

```yaml
ai:
  api_key: "hf_ваш_токен_здесь"
```

## 📝 Безопасная конфигурация:

### config.yaml (НЕ коммитится)

Содержит реальные ключи:

```yaml
ai:
  api_key: "hf_ваш_реальный_ключ"
```

### config.yaml.example (коммитится)

Содержит плейсхолдеры:

```yaml
ai:
  api_key: "your_huggingface_api_key_here"
```

## 🚨 Если случайно запушили секрет:

### 1. НЕ делайте force push на main!

### 2. Используйте BFG Repo-Cleaner или git-filter-repo:

```bash
# Установка BFG
brew install bfg

# Удаление секрета из истории
bfg --replace-text passwords.txt

# Или используйте GitHub's secret scanning
# Следуйте инструкциям по ссылке из ошибки
```

### 3. Отзовите скомпрометированный ключ:

- HuggingFace: https://huggingface.co/settings/tokens → Revoke token
- Создайте новый ключ
- Обновите `config.yaml`

## ✅ Чеклист перед пушем:

- [ ] `config.yaml` в `.gitignore`
- [ ] Нет API ключей в коммитах: `git log -p | grep "hf_"`
- [ ] `config.yaml.example` содержит только плейсхолдеры
- [ ] Документация не содержит реальных ключей
- [ ] `git status` не показывает `config.yaml`

## 🔐 Рекомендации:

1. **Используйте переменные окружения** для чувствительных данных
2. **Никогда не коммитьте** файлы с реальными ключами
3. **Регулярно ротируйте** API ключи
4. **Используйте разные ключи** для разработки и продакшена
5. **Включите GitHub Secret Scanning** в настройках репозитория

---

## 📚 Полезные ссылки:

- [GitHub Secret Scanning](https://docs.github.com/en/code-security/secret-scanning)
- [HuggingFace Tokens](https://huggingface.co/settings/tokens)
- [Git Secrets Tool](https://github.com/awslabs/git-secrets)




