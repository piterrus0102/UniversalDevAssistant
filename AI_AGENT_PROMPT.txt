================================================================================
ИНСТРУКЦИЯ ДЛЯ AI АГЕНТА
================================================================================

Привет! Ты работаешь с проектом **Universal Dev Assistant** - это AI-ассистент 
разработчика на Kotlin.

================================================================================
ЧТО НУЖНО ЗНАТЬ:
================================================================================

1. ЭТО ПРОЕКТ-СУБАГЕНТ
   Он подключается к ДРУГОМУ проекту (например, /Desktop/zayobushek) и 
   отвечает на вопросы о нем через RAG + LLM + Git MCP.

2. ТЕХНОЛОГИИ:
   - Kotlin + JVM (типобезопасность)
   - Ktor (HTTP сервер)
   - HuggingFace API (Qwen/Qwen2.5-7B-Instruct) - для LLM
   - Ollama (mxbai-embed-large) - для векторизации
   - Git - для MCP

3. ГЛАВНЫЙ ENDPOINT:
   GET /help?q=вопрос
   
   Workflow:
   Вопрос → RAG (индексирует docs/) → Ollama (векторизует) → 
   → находит релевантные документы → HuggingFace (генерирует ответ) → 
   → Git MCP (добавляет контекст) → JSON ответ

4. КОНФИГУРАЦИЯ:
   Всё в config.yaml:
   - project.path - путь к ЦЕЛЕВОМУ проекту (не к этому!)
   - ai.api_key - ключ HuggingFace (из AIAdvent4Thread)
   - vectorization.ollama_url - URL Ollama

5. ЗАПУСК:
   ./START.sh  ИЛИ  ./gradlew run

================================================================================
СТРУКТУРА КОДА (ключевые файлы):
================================================================================

Main.kt                         - Инициализация всех компонентов
server/AssistantServer.kt       - HTTP роутинг, endpoint /help
rag/RAGService.kt              - Индексация + поиск документов
rag/OllamaClient.kt            - Векторизация через Ollama
ai/HuggingFaceClient.kt        - LLM запросы к Qwen
mcp/GitMCP.kt                  - Git команды
config/ProjectConfig.kt        - Загрузка config.yaml

================================================================================
ПРИ ПОМОЩИ В РАЗРАБОТКЕ:
================================================================================

1. Если пользователь спрашивает "как это работает":
   - Объясни workflow из раздела "КАК ЭТО РАБОТАЕТ" в PROJECT_CONTEXT.txt
   - Покажи код из соответствующих файлов

2. Если нужно добавить функционал:
   - Новые endpoints → server/AssistantServer.kt
   - Новая логика поиска → rag/RAGService.kt
   - Новые git команды → mcp/GitMCP.kt

3. Если ошибки при запуске:
   - Ollama недоступна → "ollama serve"
   - Config not found → "cp config.yaml.example config.yaml"
   - Git error → проверь project.path в config.yaml

4. Если нужно подключить к другому проекту:
   - Отредактируй config.yaml:
     project:
       name: "новый-проект"
       path: "/полный/путь/к/проекту"

================================================================================
ВАЖНО:
================================================================================

- Проект НЕ привязан к zayobushek - он универсальный
- Ollama ОПЦИОНАЛЬНА (fallback на keyword search)
- HuggingFace ключ УЖЕ в config.yaml
- Векторизация = семантический поиск (не просто grep)

================================================================================
БЫСТРЫЕ КОМАНДЫ:
================================================================================

# Запуск
./START.sh

# Тест
curl http://localhost:3002/health
curl 'http://localhost:3002/help?q=структура проекта'

# Сборка
./gradlew build

================================================================================
ЧИТАЙ ПОДРОБНЕЕ:
================================================================================

PROJECT_CONTEXT.txt   - Полный контекст проекта
README.md            - Документация
INTEGRATION.md       - Интеграция с AIAdvent4Thread
READY_TO_USE.md      - Готовность к использованию

================================================================================

Теперь ты готов помогать с этим проектом! 🚀


