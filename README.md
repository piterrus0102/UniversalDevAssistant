# 🤖 Universal Dev Assistant

**Универсальный AI-ассистент разработчика** на Kotlin, который подключается к любому проекту и отвечает на вопросы о его структуре, коде и документации.

## ✨ Возможности

- 📖 **RAG (Retrieval-Augmented Generation)** - индексирует и анализирует документацию проекта
- 🔢 **Векторизация (Ollama)** - семантический поиск через embeddings (mxbai-embed-large)
- 🔧 **Git MCP** - интеграция с Git (текущая ветка, статус, коммиты)
- 🧠 **HuggingFace AI** - использует Qwen/Qwen2.5-7B-Instruct для интеллектуальных ответов
- 🌐 **HTTP API** - простой REST API для взаимодействия
- ⚙️ **Универсальность** - можно подключить к ЛЮБОМУ проекту через конфиг
- 🔌 **Интеграция** - использует компоненты из AIAdvent4Thread

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий

```bash
cd ~/Desktop
git clone <repo-url> UniversalDevAssistant
cd UniversalDevAssistant
```

### 2. Настройте конфигурацию

```bash
# Создайте config.yaml из примера
cp config.yaml.example config.yaml

# Отредактируйте config.yaml
nano config.yaml
```

**Пример config.yaml:**

```yaml
project:
  name: "zayobushek"
  path: "/Users/ruslanhafizov/Desktop/zayobushek"
  docs:
    - "README.md"
    - "docs/*.md"
    - "MIGRATION_NOTES.md"
  ignore:
    - "node_modules"
    - ".git"

ai:
  provider: "anthropic"
  model: "claude-3-5-sonnet-20241022"
  api_key_env: "ANTHROPIC_API_KEY"
  max_tokens: 2048

server:
  port: 3002
  host: "0.0.0.0"

git:
  enabled: true
```

### 3. Установите зависимости

Убедитесь, что у вас установлен **JDK 17+**:

```bash
java -version
```

### 4. Запустите Ollama

Для векторизации нужна Ollama:

```bash
# Установка (macOS)
brew install ollama

# Запуск
ollama serve

# Установка модели (в другом терминале)
ollama pull mxbai-embed-large
```

**Примечание**: Ollama опциональна. Если она недоступна, будет использоваться keyword search.

### 5. Запустите ассистента

```bash
# Сборка и запуск
./gradlew run

# Или в режиме разработки
./gradlew run --continuous
```

Вы увидите:

```
🤖 Universal Dev Assistant готов к работе!
📂 Проект: zayobushek
🌐 Сервер: http://0.0.0.0:3002

Примеры использования:
  curl 'http://localhost:3002/help?q=структура проекта'
  curl http://localhost:3002/git/status
  curl http://localhost:3002/docs
```

## 📡 API Endpoints

### `/help?q=вопрос` - Главная фишка! 🎯

Задать вопрос о проекте:

```bash
curl 'http://localhost:3002/help?q=как устроена структура проекта'
curl 'http://localhost:3002/help?q=где находится API документация'
curl 'http://localhost:3002/help?q=как добавить новый урок'
```

**Ответ:**

```json
{
  "project": "zayobushek",
  "question": "как устроена структура проекта",
  "answer": "Проект состоит из двух основных частей:\n\n1. **Backend** (backend/api/)...",
  "sources": [
    "README.md",
    "docs/01_СТРУКТУРА_ПРОЕКТА.md"
  ]
}
```

### `/git/status` - Статус репозитория

```bash
curl http://localhost:3002/git/status
```

**Ответ:**

```json
{
  "branch": "main",
  "status": " M backend/api/index.php\n M frontend/src/App.jsx"
}
```

### `/git/branch` - Текущая ветка

```bash
curl http://localhost:3002/git/branch
```

**Ответ:**

```json
{
  "branch": "main"
}
```

### `/git/info` - Полная информация о Git

```bash
curl http://localhost:3002/git/info
```

**Ответ:**

```json
{
  "isGitRepo": true,
  "currentBranch": "main",
  "lastCommit": "abc123 - Added new feature (User, 2 hours ago)",
  "modifiedFiles": ["backend/api/index.php", "frontend/src/App.jsx"],
  "status": "M backend/api/index.php...",
  "remote": "origin\tgit@github.com:user/repo.git (fetch)"
}
```

### `/docs` - Список документов

```bash
curl http://localhost:3002/docs
```

**Ответ:**

```json
{
  "count": 5,
  "documents": [
    {
      "path": "README.md",
      "lines": 150,
      "size": 8192
    },
    {
      "path": "docs/01_СТРУКТУРА_ПРОЕКТА.md",
      "lines": 80,
      "size": 4096
    }
  ]
}
```

### `/docs/{path}` - Содержимое документа

```bash
curl http://localhost:3002/docs/README.md
```

### `/health` - Проверка работоспособности

```bash
curl http://localhost:3002/health
```

**Ответ:**

```json
{
  "status": "ok",
  "project": "zayobushek",
  "docsCount": 5,
  "gitEnabled": true
}
```

## 🎯 Примеры использования

### 1. Вопрос о структуре проекта

```bash
curl 'http://localhost:3002/help?q=как устроена структура проекта'
```

### 2. Вопрос об API

```bash
curl 'http://localhost:3002/help?q=где документация API'
```

### 3. Проверка Git статуса

```bash
curl http://localhost:3002/git/status
```

### 4. Вопрос о конкретном файле

```bash
curl 'http://localhost:3002/help?q=что делает AuthController'
```

## 🔧 Как подключить к другому проекту

Просто измените `config.yaml`:

```yaml
project:
  name: "мой-другой-проект"
  path: "/Users/me/Desktop/my-project"
  docs:
    - "README.md"
    - "docs/**/*.md"
```

И перезапустите!

## 🏗️ Архитектура

```
┌─────────────────────────────────────┐
│  HTTP Client (curl, browser, app)  │
└──────────────┬──────────────────────┘
               │ HTTP API
               ▼
┌─────────────────────────────────────┐
│      AssistantServer (Ktor)         │
│  • GET /help?q=вопрос               │
│  • GET /git/status                  │
│  • GET /docs                        │
└──┬─────────┬─────────┬──────────────┘
   │         │         │
   ▼         ▼         ▼
┌────────┐ ┌───────┐ ┌─────────────┐
│ RAG    │ │ Git   │ │ Claude API  │
│ Service│ │ MCP   │ │ Client      │
└────────┘ └───────┘ └─────────────┘
   │         │              │
   ▼         ▼              ▼
📁 Docs   📂 .git      ☁️ Anthropic
```

## 📦 Зависимости

- **Kotlin** 1.9.22
- **Ktor** 2.3.7 (HTTP сервер)
- **kotlinx.serialization** (JSON)
- **KAML** (YAML парсинг)
- **kotlin-logging** (логирование)

## 🛠️ Команды разработки

```bash
# Запуск
./gradlew run

# Сборка
./gradlew build

# Запуск с автоперезагрузкой
./gradlew run --continuous

# Создание JAR
./gradlew jar
```

## 📋 Требования

- **JDK 17+**
- **Git** (для Git MCP)
- **Claude API ключ** (бесплатный на https://console.anthropic.com/)

## 🎓 Для AIAdvent

Этот проект выполняет требования **Дня 20: Ассистент разработчика**:

- ✅ RAG для подключения к документации проекта (README + docs/)
- ✅ MCP для git-репозитория (git branch, git status, и др.)
- ✅ Команда `/help` отвечает на вопросы о проекте

**Достаточный результат для зачета:**
- ✅ README + папка docs/ в RAG
- ✅ MCP для получения текущей ветки (git branch)
- ✅ Команда /help отвечает на вопросы о структуре проекта

## 📝 Лицензия

MIT

## 🤝 Автор

Создано для AIAdvent4Thread - Day 20

---

**Enjoy! 🚀**

